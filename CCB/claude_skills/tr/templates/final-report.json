{
  "proto": "autoflow.fileops.v1",
  "id": "TR-FINAL-REPORT",
  "purpose": "finalize_step",
  "summary": "Write final task summary report",
  "done": ["Report written to final/{{taskName}}-report.md"],
  "ops": [
    { "op": "run", "cmd": "mkdir -p final", "cwd": "." },
    { "op": "write_file", "path": "final/{{taskName}}-report.md", "content": "{{reportContent}}" }
  ],
  "report": { "changedFiles": true },
  "_meta": {
    "placeholders": {
      "taskName": "from state.json.taskName (sanitized for filename)",
      "reportContent": "markdown report content (generated by Claude, see flow.md Step 10.3)"
    }
  }
}
