# mock_project_v51 fixtures

This fixture set is used by `tests/integration/test_e2e_v51.sh`.

- `tasks/*.json` are legacy-style inputs that require migration to v5.1.
- `repo/` is created dynamically by the test script (initialized as a git repo).
